name: bluesky-handball-bot

on:
  schedule:
    - cron: '7,37 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: R einrichten
        uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - name: Pakete installieren
        run: install.packages(c("atrrr", "xml2"))
        shell: Rscript {0}

      - name: Bot ausfuehren
        env:
          BSKY_USER: ${{ secrets.BSKY_USER }}
          BSKY_APP_PASSWORD: ${{ secrets.BSKY_APP_PASSWORD }}
        run: Rscript post_handball.R

      - name: Status und Feed-Kopie zurueckschreiben
        run: |
          git config user.name  "handball-bot"
          git config user.email "bot@users.noreply.github.com"
          git add feed/ state/
          git diff --staged --quiet || git commit -m "Update feed + state [skip ci]"
          git push
